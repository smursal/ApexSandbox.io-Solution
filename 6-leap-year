public Boolean isLeapYear(Integer year) {
    Integer results1 = Math.mod(year, 100);
    Integer results1 = Math.mod(year, 400);
    if ( results1 == 0 && results1 == 0) ){
        return true;
    }
    else false;
}
