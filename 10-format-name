public String formatName(String firstName, String lastName) {
    if (String.IsBlank(firstName) && lastName != null ) {
        return lastName;


    }

    if (firstName != null && String.IsBlank(lastName) ){
        return firstName;

    }
    else { 

        return lastName+', '+ firstName;
    }
    //code here
}